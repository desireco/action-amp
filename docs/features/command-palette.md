---
slug: command-palette
title: "Command palette + full-text search (⌘\\)"
feature_area: cross-cutting
status: missing
spec: command-palette-search.md   # ready
verified: 2026-07-03
---

# Command palette + search

**Wanted.** `⌘\` fuzzy jump/run over items, projects, goals, lenses, views,
actions + full-text search across **all** tasks (open + done). The two Pro-tier
features most likely to justify the price to an existing user.

**Today.** No code. `⌘K` is capture (available to all). The only mentions of
"command palette" are comments (ProGate lists it as "future Pro feature"). No
`cmdk`, no palette component.

**Spec.** `docs/specs/command-palette-search.md` (`ready`). Pro-gated. Depends
on `entitlement-enforcement` (shipped).

**Why it matters.** Per ROADMAP §Next: depth feature that earns the price.
