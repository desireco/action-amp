.wasp/
node_modules/

# Build artifact — emitted by vite.config.ts from the same git SHA that defines
# __APP_VERSION__. Served at /version.json for the client-side update poll.
public/version.json

# Ignore all dotenv files by default to prevent accidentally committing any secrets.
# To include specific dotenv files, use the `!` operator or adjust these rules.
.env
.env.*

# Don't ignore example dotenv files.
!.env.example
!.env.*.example

# Isolated e2e worktree env (auto-generated by scripts/e2e-setup.sh).
.e2e.env

test-results/

# Playwright scratch (page snapshots/console logs, not source)
.playwright-cli/
.playwright-mcp/
