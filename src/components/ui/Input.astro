---
import { Input as ShadcnInput } from '@/components/ui/input';

interface Props {
    class?: string;
    type?: astroHTML.JSX.HTMLInputTypeAttribute;
    [key: string]: any;
}

const { class: className = "", type = "text", ...rest } = Astro.props;
---

<ShadcnInput client:idle type={type} class={className} {...rest} />
