# Project Rules

## Dev Server
- Always check `http://localhost:4321/` before starting a new dev server.
- If it is running, reuse it instead of launching another instance.
- Only start a new dev server if `http://localhost:4321/` is not responding.
