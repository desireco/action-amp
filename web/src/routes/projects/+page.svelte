<script lang="ts">
  // Projects list host (/projects — S5). The Planning eyebrow, cards, and
  // composer live in the view; the route is a thin host like every app route.
  import "../../lib/components/ui/Button.css";
  import ProjectsView from "../../lib/components/projects/ProjectsView.svelte";
</script>

<ProjectsView />
