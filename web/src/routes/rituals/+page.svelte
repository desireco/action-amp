<script lang="ts">
  // Rituals list host (/rituals — the Planning page). A thin host like every
  // app route; the view + store carry the surface.
  import RitualsView from "../../lib/components/rituals/RitualsView.svelte";
</script>

<RitualsView />
