<script lang="ts">
  // Projects list host (/do/projects — S5). The Planning eyebrow, cards, and
  // composer live in the view; the route is a thin host like every /do route.
  import "../../../lib/styles/Button.css";
  import ProjectsView from "../../../lib/components/projects/ProjectsView.svelte";
</script>

<ProjectsView />
