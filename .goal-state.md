# Goal state

objective: Clear all anti-slop Oxlint findings in L1 test files (all *.test.ts(x) + src/test/* except src/billing/ and src/auth/), committing each batch, per docs/backlog/anti-slop-cleanup.md
started: 2026-08-17

## Done-conditions (build-verified; lint/test output is the judge)

- [ ] `npx oxlint` in webapp reports 0 findings for L1-owned test files (excluding skipped no-module-mocking)
- [ ] Vitest green for all touched test files
- [ ] Total repo lint count never goes above 690
- [ ] Each logical batch committed separately, direct to main

## Excluded (B9 batch, not fixed in L1)

- All no-module-mocking findings (~26) — deferred to B9

## L1 file batches (by feature area)

1. tasks tests (operations.test.ts, operationsCore.test.ts, activePool.test.ts) — pending
2. onboarding tests (operations.test.ts) — pending
3. inbox tests (operations.test.ts, operations.capture.test.ts, TriagePage.test.tsx, InboxPage.test.tsx) — pending
4. components/ui tests (usePropertyKeys.test.tsx, FocusMode.test.tsx, CapturePopover.test.tsx) — pending
5. share tests (shareCapture.test.ts, manifest.test.ts) — pending
6. lenses tests (LensesPage.test.tsx) — pending
7. attachments tests (serveAttachment.test.ts) — pending
8. shared tests (imageFiles.test.ts) — pending
9. simpleLists tests (operations.test.ts, SimpleListPage.test.tsx) — pending
10. app tests (operations.test.ts, AppShell.test.tsx, useKeyboardShortcuts.test.tsx, taskContext.test.ts) — pending
11. admin tests (userManagementCore.test.ts) — pending
12. projects tests (operations.test.ts, ProjectDetailPage.test.tsx) — pending
13. search tests (paletteAvailability.test.ts, CommandPalette.test.tsx, operationsCore.test.ts) — pending
14. lists tests (UpcomingPage.test.tsx, SomedayPage.test.tsx) — pending
15. goals tests (operations.test.ts, GoalDetailPage.test.tsx) — pending
16. feedback tests (operationsCore.test.ts, captureContext.test.ts) — pending
17. src/test/* files (setup.ts, mockContext.ts) — pending
18. App.test.tsx — pending

## Key files

- docs/backlog/anti-slop-cleanup.md — the backlog
- webapp/oxlint.config.ts — lint config

## Last judge verdict

n/a (build-verified conditions; commands are the judge)

## Pick up here

Read webapp/src/tasks/operations.test.ts and start adding SAFETY comments to type assertions (batch 1).
